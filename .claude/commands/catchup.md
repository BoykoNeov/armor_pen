---
description: Summarize recent repo activity and what's likely next
---

Give me a fast orientation on this repo's current state:

1. `git log --oneline -15` — recent commits.
2. `git status` — uncommitted work in progress.
3. Read the root `CLAUDE.md` §3 (layout) and skim the per-directory `CLAUDE.md`
   status tables to see which pieces are working vs. stubbed.
4. Summarize in a few bullets: what's done, what's stubbed, and the most likely
   next step (per the build order in `solver/CLAUDE.md`). Keep it terse.

Do not start any implementation — this is orientation only.
